{
    // JS로 컴파일하고 싶은 모든 디렉토리
    "include": [
        "src" // ts가 src의 모든 파일 확인
    ],
    "compilerOptions": {
        // 컴파일 된 JS 파일이 생성될 디렉토리
        "outDir": "build",
        // 어떤 버전의 JS로 컴파일 할지
        "target": "ES6",
        // TS에게 어떤 API를 사용하고 어떤 환경에서 코드를 실행할지 명시
        // target runtime 환경
        // 브라우저에서 동작할 거라면 "DOM"
        "lib": ["ES6", "DOM"],
        "strict": true,
        "allowJs": true
    }
}